@page "/admin"
@attribute [Authorize(Roles = "Admin")]

@layout AdminLayout

<div class="hd-admin-welcome-container">
    <div class="hd-admin-welcome-card">
        <h1 class="hd-title hd-text-center">Chào mừng đến với trang Quản trị</h1>
        <p class="hd-admin-welcome-message">
            Bạn đang ở khu vực quản trị của Hồng Đức Fashion.<br />
            Vui lòng sử dụng menu bên trái để quản lý sản phẩm, danh mục, đơn hàng và các chức năng khác.
        </p>
        <div class="hd-admin-welcome-icon">
            <svg width="64" height="64" fill="none" viewBox="0 0 64 64">
                <circle cx="32" cy="32" r="32" fill="#e57373" />
                <path d="M32 18v28M18 32h28" stroke="#fff" stroke-width="4" stroke-linecap="round" />
            </svg>
        </div>
    </div>
</div>